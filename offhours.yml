policies:

#==================
# EC2
#==================

# turn instances off during non-business hours
  policies:
  - name: turn-off
    resource: ec2
    filters:
      - "tag:Custodian": present
    actions:
      - stop